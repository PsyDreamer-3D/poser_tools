# Vendored FBX import library (io_scene_fbx v5.12.3).
# Only the import_fbx module and its dependencies are used by Poser Tools.
# Operator classes are intentionally NOT defined here to avoid Blender's
# "unregistered class" warnings — we register our own operator instead.
